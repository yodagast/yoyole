"""微信支付接入配置（复制到 .env 并填入真实值；本文件本身不含任何密钥）

文档：docs/wechatpay.md

⚠️ 密钥类配置有两种提供方式，**推荐用文件**（不进环境变量、不会出现在进程列表里）：
  1. 文件方式：把商户 API 证书私钥放到 cert/apiclient_key.pem，
     把微信支付公钥放到 cert/wxpay_public_key.pem（cert/ 已在 .gitignore 中）
  2. 内容方式：把 PEM 全文写进 WECHATPAY_PRIVATE_KEY_PEM / WECHATPAY_PUBLIC_KEY_PEM，
     换行用 \\n 转义（适合不能挂文件的部署）

=== 必填项（缺任意一项，微信通道不会出现在支付方式里）===

# 商户号（商户平台 → 账户中心 → 商户信息，10 位数字）
WECHATPAY_MCHID=

# 已与商户号绑定的 appid（公众号/开放平台/小程序任意一个；Native 下单也必填）
# 绑定关系：商户平台 → 产品中心 → AppID 账号管理
WECHATPAY_APPID=

# APIv3 密钥：32 位，商户平台 → 账户中心 → API 安全 → 设置 APIv3 密钥
# ⚠️ 回调报文的解密密钥，丢失只能重设（重设后旧密钥立即失效）
WECHATPAY_API_V3_KEY=

# 商户 API 证书序列号（申请证书时显示，40 位十六进制）
WECHATPAY_CERT_SERIAL_NO=

=== 证书/公钥（默认读文件，文件不存在时才看内容变量）===

WECHATPAY_PRIVATE_KEY_PATH=cert/apiclient_key.pem
WECHATPAY_PUBLIC_KEY_PATH=cert/wxpay_public_key.pem
# 微信支付公钥 id（形如 PUB_KEY_ID_0000000000000024101100397200000006）
WECHATPAY_PUBLIC_KEY_ID=
# 若用「平台证书」而非公钥：把证书按序列号命名放进该目录（如 cert/platform/5157F09E...pem）
WECHATPAY_PLATFORM_CERT_DIR=cert/platform

=== 可选 ===

# API 域名：主域名就近接入 / 备域名 api2.mch.weixin.qq.com 异地接入（跨城冗灾）
WECHATPAY_API_BASE=https://api.mch.weixin.qq.com
# 单笔订单可支付时长（分钟），传给 time_expire
WECHATPAY_PAY_EXPIRE_MINUTES=120

=== 开启通道 ===
# 微信支付必须显式打开开关，且凭据齐备才会出现在 /api/payments/methods
PAYMENT_GATEWAY_ENABLED={"mock": true, "alipay": false, "wechat": true, "stripe": false, "paypal": true}